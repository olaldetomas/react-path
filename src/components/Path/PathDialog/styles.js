import { makeStyles } from '@material-ui/core/styles'

export default makeStyles((theme) => ({
  dialogContent: {
    width: 350,
  },
  input: {
    marginTop: 5
  },
  formControl: {
    minWidth: '100%',
    marginBottom: 20,
    marginTop: 5
  }
}))
